import React from 'react';

const Navigation = () => {
	return (
		<nav style={{display: 'flex', justifyContent: 'flex-end'}}>
			<p className='rat f3 link dim black underline pa3 mointer'>Sign out</p>
		</nav>
	);
}

export default Navigation;